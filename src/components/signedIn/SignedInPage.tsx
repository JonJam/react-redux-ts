import * as React from "react";

export default class SignedInPage extends React.Component {
  public render() {
    return (
      <div>
        <h1 className="sr-only">Signed in page</h1>
        You are signed in.
      </div>
    );
  }
}
